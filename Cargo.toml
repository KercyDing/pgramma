[package]
name = "pgramma"
version = "0.1.0"
edition = "2024"

[dependencies]
redb = "3"
serde = { version = "1", features = ["derive"] }
bincode = "2"
chrono = { version = "0.4", features = ["serde"] }
thiserror = "2"

[dev-dependencies]
tempfile = "3"
